---
- name: Sample ansible playbook for installing apache2
  hosts: worker1
  become: yes
  tasks:
    - name: Install apache2
      apt:
        name: apache2
        state: present
    
    - name: Start apache2 service
      service:
        name: apache2
        state: started
        enabled: yes
