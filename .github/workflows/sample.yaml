name: this is a sample workflow
on:
    push:
        branches:
            - main
    workflow_dispatch:
jobs:
    Build:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v2
            
            - name: list contents of a folder
              run: ls -la
            
            - name: greet hello world
              run: echo "Hello, World!"
            
            - name: run a script
              run: |
                echo "This is a multi-line script."
                echo "You can run multiple commands here."
                echo "This is useful for complex build steps."